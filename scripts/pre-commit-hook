#!/usr/bin/env bash
set -euo pipefail
# RALPH_RUST_MANAGED_HOOK - generated by ralph
# Repository root (embedded at install time using libgit2): /Users/mistlight/Projects/RalphWithReviewer/wt-34-tests/

if [[ -f "/Users/mistlight/Projects/RalphWithReviewer/wt-34-tests//.no_agent_commit" ]]; then
  echo "✋ Commit blocked (agent phase): .no_agent_commit exists."
  exit 1
fi

orig="/Users/mistlight/Projects/RalphWithReviewer/.git/worktrees/wt-34-tests/hooks/pre-commit.ralph.orig"
if [[ -f "$orig" ]]; then
  exec "$orig" "$@"
fi

exit 0
