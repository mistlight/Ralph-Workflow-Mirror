{# ============================================================================ #}
{# Template: planning_xml.txt                                                  #}
{# Version: 1.0                                                                 #}
{# ============================================================================ #}
{#                                                                             #}
{# PURPOSE:                                                                    #}
{#   Prompt for planning phase to create detailed implementation plans.         #}
{#   Uses XML output format for reliable parsing.                              #}
{#                                                                             #}
{# VARIABLES:                                                                  #}
{#   {{PROMPT}} - User requirements from PROMPT.md (required)                   #}
{#                                                                             #}
{# OUTPUT FORMAT:                                                              #}
{#   <ralph-plan>                                                              #}
{#     <ralph-summary>One paragraph explanation</ralph-summary>                 #}
{#     <ralph-implementation-steps>Numbered, actionable steps</ralph-implementation-steps> #}
{#     <ralph-critical-files>List of key files (optional)</ralph-critical-files> #}
{#     <ralph-risks-mitigations>Risks and mitigations (optional)</ralph-risks-mitigations> #}
{#     <ralph-verification-strategy>How to verify (optional)</ralph-verification-strategy> #}
{#   </ralph-plan>                                                              #}
{#                                                                             #}
{# EXTRACTION:                                                                 #}
{#   Parsed by extract_plan_xml() in xml_extraction_plan.rs                    #}
{#   Validated by validate_plan_xml() in xsd_validation_plan.rs                #}
{# ============================================================================ #}

You are in PLANNING MODE. Create a detailed implementation plan.

CRITICAL: This is a READ-ONLY planning task. You are STRICTLY PROHIBITED from:
- Creating, modifying, or deleting any files
- Running any commands that modify system state
- Installing dependencies or packages

You MAY use read-only operations: reading files, searching code, listing directories.

═══════════════════════════════════════════════════════════════════════════════
PHASE 1: UNDERSTANDING
═══════════════════════════════════════════════════════════════════════════════

USER REQUIREMENTS:
{{PROMPT}}

Understand:
- The Goal: What is the desired end state?
- Acceptance Checks: What specific conditions must be satisfied?
- Constraints: Any requirements, limitations, or quality standards mentioned?

If requirements are ambiguous, note them for clarification in the plan.

═══════════════════════════════════════════════════════════════════════════════
PHASE 2: EXPLORATION
═══════════════════════════════════════════════════════════════════════════════
Explore the codebase using read-only tools to understand:
- Current architecture and patterns used
- Relevant existing code that will need changes
- Dependencies and potential impacts
- Similar implementations that can serve as reference
- Test patterns and coverage expectations

Be thorough. Quality exploration leads to better plans.

═══════════════════════════════════════════════════════════════════════════════
PHASE 3: DESIGN
═══════════════════════════════════════════════════════════════════════════════
Design your implementation approach:
- What changes need to be made and in what order?
- Are there multiple valid approaches? Evaluate trade-offs.
- What are the potential risks or challenges?
- How does this integrate with existing code patterns?

═══════════════════════════════════════════════════════════════════════════════
PHASE 4: REVIEW
═══════════════════════════════════════════════════════════════════════════════
Validate your plan against the requirements:
- Does the approach satisfy ALL acceptance checks?
- Are there edge cases or error scenarios to handle?
- Is the plan specific enough to implement without ambiguity?
- Have you identified the correct files to modify?

═══════════════════════════════════════════════════════════════════════════════
PHASE 5: WRITE PLAN
═══════════════════════════════════════════════════════════════════════════════
OUTPUT your plan using ONLY these XML tags:

<ralph-plan>
<ralph-summary>One paragraph explaining what will be done and why.</ralph-summary>
<ralph-implementation-steps>Numbered, actionable steps. Be specific about:
- What each step accomplishes
- Which files are affected
- Dependencies between steps</ralph-implementation-steps>
<ralph-critical-files>List 3-5 key files that will be created or modified:
- `path/to/file.rs` - Brief justification for why this file needs changes</ralph-critical-files>
<ralph-risks-mitigations>Challenges identified during exploration and how to handle them.</ralph-risks-mitigations>
<ralph-verification-strategy>How to verify acceptance checks are met:
- Specific tests to run
- Manual verification steps
- Success criteria</ralph-verification-strategy>
</ralph-plan>

### CRITICAL OUTPUT INSTRUCTIONS:

1. Start your response IMMEDIATELY with <ralph-plan>
2. NO text before <ralph-plan>
3. NO text after </ralph-plan>
4. All tags except <ralph-summary> and <ralph-implementation-steps> are optional
5. Use actual newlines inside elements for readability
6. NO markdown code fences around the XML
7. NO analysis or thinking outside the XML

### CORRECT OUTPUT EXAMPLE:

<ralph-plan>
<ralph-summary>Add XML-based communication with XSD validation for development and fixer agents,
following the same pattern as the commit agent. This ensures consistent behavior and guarantees
that agents write all expected data.</ralph-summary>

<ralph-implementation-steps>1. Create XSD schema files for plan, issues, and fix_result formats
2. Create XML extraction modules for each format with 4-strategy extraction
3. Create XSD validation modules for each format
4. Update development phase to use XML extraction and validation with retry loop
5. Update review phase to use XML extraction and validation
6. Create prompt templates with XML format specifications</ralph-implementation-steps>
<ralph-critical-files>- `ralph-workflow/src/files/llm_output_extraction/plan.xsd` - XSD schema for plan validation
- `ralph-workflow/src/phases/development.rs` - Main orchestration for planning phase
- `ralph-workflow/src/prompts/templates/planning_xml.txt` - Prompt template with XML format</ralph-critical-files>
<ralph-risks-mitigations>Risk: AI agents may struggle with XML format.
Mitigation: Use proven retry-with-XSD-error pattern from commit agent (up to 10 retries).
Provide clear examples in prompt templates.</ralph-risks-mitigations>
<ralph-verification-strategy>- Run unit tests for all XSD validation modules
- Run integration tests for planning phase with XSD validation
- Test that invalid XML triggers retry with specific XSD error
- Verify that valid XML is accepted and processed correctly</ralph-verification-strategy>
</ralph-plan>
